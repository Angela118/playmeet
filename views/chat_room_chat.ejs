<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <meta name="description" content="">
    <meta name="author" content="">

    <link href="../css/bootstrap.min.css" rel="stylesheet">
    <link href="../css/chat_room.css" rel="stylesheet">
    <link href="../css/common.css" rel="stylesheet">
    <link href="../css/FRAME.css" rel="stylesheet">

    <script src="../js/jquery.js"></script>
    <script src="../js/bootstrap.bundle.min.js"></script>

    <script type="text/javascript">
        jQuery.noConflict();
        jQuery(document).ready(function () {
            jQuery('.message').click(function (e) {
                // location.href = '/chat';
            });
        });
        function goBack(){
            window.history.back();
        }
    </script>

    <script>

        function createRoom(){
            var roomId = $('#roomIdInput').val();
            var roomName = $('#roomNameInput').val();
            var id = $('#idInput').val();
            var output = {
                command:'create',
                roomId:roomId,
                roomName:roomName,
                roomOwnoer:id
            };
            socket.emit('room', output);
        }
        function updateRoom(){
            var roomId = $('#roomIdInput').val();
            var roomName = $('#roomNameInput').val();
            var id = $('#idInput').val();
            var output = {
                command:'update',
                roomId:roomId,
                roomName:roomName,
                roomOwnoer:id
            };
            socket.emit('room', output);
        }
        function deleteRoom(){
            var roomId = $('#roomIdInput').val();
            var id = $('#idInput').val();
            var output = {
                command:'delete',
                roomId:roomId,
                roomName:'',
                roomOwnoer:id
            };
            socket.emit('room', output);
        }
        function joinRoom(){
            var roomId = $('#roomIdInput').val();
            var output = {
                command:'join',
                roomId:roomId
            };
            socket.emit('room', output);
        }
        function leaveRoom(){
            var roomId = $('#roomIdInput').val();
            var output = {
                command:'leave',
                roomId:roomId
            };
            socket.emit('room', output);
        }
        function println(data){      //상태 프린트
            console.log(data);
            $('#results').append('<p>' + data + '</p>');
        }

    </script>




    <title>채팅방 목록</title>
</head>
<body class="whiteB">

<!--메뉴 영역 시작-->
<!--여기선 메뉴명 채팅방 목록으로 되어 있음-->

<nav class="navbar blueB">
    <div class="top">
        <div>
            <img src="../img/left-arrow.png" alt="" width="20rem" height="20rem" style="margin-right:3rem" onclick="goBack();">

            <p class="logo ml-sm-3">채팅방 목록</p>
        </div>

        <div>
            <a href="/chat"><img src="../img/envelope.png" class="menu_img" width="20rem" height="20rem" style="margin-right:1rem"></a>

            <img src="../img/menu.png" width="20rem" height="20rem" class="navbar-toggler" data-toggle="collapse" data-target="#navbarsExample01" aria-controls="navbarsExample01" aria-expanded="false" aria-label="Toggle navigation">
        </div>
    </div>

    <div class="collapse navbar-collapse" id="navbarsExample01">
        <div class="mypage">
            <img src="../profile_img/<%= profile_img %>" id="profile_pic" style="margin-right: .2rem; width:3rem; height: 3rem">
            <span id="name" style="font-weight: bold; font-size: 1.2rem; margin-left: .5rem"><%= teamname %> 팀</span>
            <a href="/teamprofile"><button class="btn btn-sm btn-warning" type="submit" style="margin-right: .5rem">내 정보</button></a>
            <a href="/uploadimg"><button class="btn btn-sm btn-warning" type="submit" style="margin-right: .5rem">프로필 사진 변경</button></a>
            <a href="/logout"><button class="btn btn-sm btn-warning" type="submit">로그아웃</button></a>
        </div>

        <ul class="navbar-nav mr-auto">
            <li class="nav-item">
                <a class="nav-link" href="/">홈 (매칭 팀 검색)</a>
            </li>
            <li class="nav-item" style="display:flex; align-items:center">
                <span style="margin-bottom: 0; padding: .5rem 0rem; color:white">우리 팀</span>
                <!--<hr align="right" style="border: solid 0.1rem white; width: 70%;">-->
            </li>

            <li class="nav-item">
                <a class="nav-link" href="/teamreceivedreview" style="margin-left:2rem">우리 팀 평가 점수</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="/teamschedule" style="margin-left:2rem">경기 스케줄</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="/matchapplication">매칭 등록</a>
            </li>

            <li class="nav-item">
                <a class="nav-link" href="/contact">개발자에게 연락</a>
            </li>
        </ul>
    </div>
</nav>

<div class="py-4 text-center"></div>

<!--메뉴 영역 끝-->

<div class = "blueB chat_tab">
    <div onclick="location='./chatroomchat'">채팅</div>
    <div onclick="location='./chatroommessage'">메시지</div>
</div>

<div class="container" style="padding-top:6rem">
    <div class="reco" style="margin-top: -1rem">
        <!--event_data는 내게 신청한 상대팀 정보-->
        <!--event_data.others는 선택된 (내가 올렸던) 경기 날짜 & 시간 등 들어있음 -->

        <% if(event_data.length == 0){ %>
        <p style="font-weight: bold">성사된 매칭이 없습니다.</p>
        <%}else{
        var count = 0; // 띄워지는 값 count

        for(var i = 0; i < event_data.length; i++) {
            // var email = event_data[i].email;
            // var otherEmail = event_data[i].otherEmail;
            // var match_success = event_data[i].match_success;
            // var otherTeamname = event_data[i].otherTeamname;
            // var event_date = event_data[i].event_date;
            // var event_time = event_data[i].event_time;


        // 다 돌았는데 표시할 게 없으면
        if((i == event_data.length -1 ) && (count == 0)) {
            %> <p style="font-weight: bold">성사된 매칭이 없습니다.</p> <%

            // matchsuccess=0인것만 메시지함에서 띄우기
        }else if(event_data[i].match_success != 0) {
        %>

        <script>
            function clicked(email, otherEmail, match_success, otherTeamname, event_date, event_time, index) {
                document.getElementsByName('email')[index].value = email;
                document.getElementsByName('otherEmail')[index].value = otherEmail;
                document.getElementsByName('match_success')[index].value = match_success;
                document.getElementsByName('otherTeamname')[index].value = otherTeamname;
                document.getElementsByName('event_date')[index].value = event_date;
                document.getElementsByName('event_time')[index].value = event_time;
                // document.getElementsByName('chatIndex')[index].value = index;

                alert('email : ' + email + ', otherEmail : ' + otherEmail + ', index : ' + index + ', match_success : ' + match_success + ', event_date : ' + event_date + 'event_time : ' + event_time);

                /*                if(match_success == 0) {
                                    alert("아직 매칭이 완료되지 않았습니다.");
                                    location.href="/chatroomchat";
                                } else*/
                document.getElementsByName('formPost')[index].submit();
            }
        </script>

        <form action="/chatroomchat" method="post" name="formPost">
            <input type="hidden" name="email" id="email">
            <input type="hidden" name="otherEmail" id="otherEmail">
            <input type="hidden" name="match_success" id="match_success">
            <input type="hidden" name="otherTeamname" id="otherTeamname">
            <input type="hidden" name="event_date" id="event_date">
            <input type="hidden" name="event_time" id="event_time">
            <!--<input type="hidden" name="chatIndex" id="chatIndex">-->
        </form>
        <div class="message" onclick="clicked('<%= event_data[i].email %>','<%=event_data[i].otherEmail%>', '<%= event_data[i].match_success %>', '<%= event_data[i].otherTeamname %>', '<%= event_data[i].event_date %>', '<%= event_data[i].event_time %>', '<%= count%>')">
            <div class="chat_room_content">
                <div><br> <b><%= event_data[i].event_date%></b> <%= event_data[i].event_time%>시 경기 with <%= event_data[i].otherTeamname%>팀<br></div>
            </div>
        </div>

        <hr class="mb-4">

        <%
            count++;
        }
        }
        }
        %>
    </div>
</div>
</body>
</html>